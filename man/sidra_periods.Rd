% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{sidra_periods}
\alias{sidra_periods}
\title{List the periods available for a SIDRA table}
\usage{
sidra_periods(
  table,
  refresh = FALSE,
  cache = FALSE,
  ttl = 1800,
  cache_dir = NULL
)
}
\arguments{
\item{table}{A single SIDRA table code.}

\item{refresh}{A single logical value. When caching is enabled, bypass and
replace an existing entry.}

\item{cache}{A single logical value. Cache normalized catalog or metadata
responses on disk. The default is \code{FALSE}, preserving live behavior.}

\item{ttl}{A positive cache lifetime in seconds. The default is 30 minutes.}

\item{cache_dir}{Optional cache directory. By default, the platform-specific
user cache directory for \code{sidrar} is used.}
}
\value{
A base \code{\link[=data.frame]{data.frame()}} with columns \code{table_id}, \code{period_id},
\code{period_name}, \code{alternative_names}, and \code{modified}. The
\code{alternative_names} column is a base list-column of character vectors.
}
\description{
List the periods available for a SIDRA table
}
\details{
Data come from the official table-specific version 3 period
endpoint. Period identifiers are kept as character strings so leading
zeros and non-calendar codes cannot be lost.
}
\examples{
\dontrun{
sidra_periods(7060)
}
}
\seealso{
\code{\link[=sidra_metadata]{sidra_metadata()}}

Other SIDRA discovery functions:
\code{\link[=sidra_catalog]{sidra_catalog()}},
\code{\link[=sidra_locations]{sidra_locations()}},
\code{\link[=sidra_metadata]{sidra_metadata()}}
}
\author{
Renato Prado Siqueira \email{rpradosiqueira@gmail.com}
}
\concept{SIDRA discovery functions}
\keyword{IBGE}
\keyword{sidra}
