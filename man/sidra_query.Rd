% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{sidra_query}
\alias{sidra_query}
\title{Build a SIDRA query without downloading values}
\usage{
sidra_query(
  x,
  variable = "allxp",
  period = "last",
  geo = "Brazil",
  geo.filter = NULL,
  classific = "all",
  category = "all",
  header = TRUE,
  format = 4,
  digits = "default",
  api = NULL,
  value_type = c("numeric", "character", "both"),
  geo_view = NULL,
  include_extinct = FALSE
)
}
\arguments{
\item{x}{A numeric SIDRA table code. It may be omitted when \code{api} is used.}

\item{variable}{A vector of variable codes. The special selections \code{"all"}
and \code{"allxp"} are also accepted.}

\item{period}{A character vector of period codes, \code{"all"}, or a single
named value such as \code{c(last = 12)} or \code{c(first = 5)}.}

\item{geo}{A character vector with geographic aliases or \code{nNN} level codes.
Aliases and codes are case-insensitive.}

\item{geo.filter}{A list of geographic filters corresponding to \code{geo}.
Names may be aliases or \code{nNN} codes and are case-insensitive.}

\item{classific}{A vector of classification codes.}

\item{category}{\code{"all"} or a list of categories for each classification.}

\item{header}{Logical. Should the API include its header record?}

\item{format}{An integer from 1 to 4 controlling descriptor fields.}

\item{digits}{\code{"default"}, \code{"max"}, or an integer from 0 to 9.}

\item{api}{A relative SIDRA API path or complete official values URL. When
supplied, the other URL-building arguments are ignored.}

\item{value_type}{Preferred value representation for a later collection:
\code{"numeric"}, \code{"character"}, or \code{"both"}. It does not change the URL.}

\item{geo_view}{Optional numeric SIDRA territorial-view code. Territorial
views (\code{G}) cannot be combined with \code{geo} or \code{geo.filter} (\code{N}).}

\item{include_extinct}{Logical. Add \verb{/u/y} to an \code{N} query so extinct
territorial units may be returned. It cannot be used with \code{geo_view}.}
}
\value{
A list of class \code{sidra_query} with stable \code{url}, \code{header}, and
\code{parameters} components. No values are downloaded.
}
\description{
Constructs and validates a SIDRA values URL. This is useful for inspecting
a request or passing it to \code{\link[=sidra_plan]{sidra_plan()}} before any values are downloaded.
}
\examples{
query <- sidra_query(
  1612,
  variable = 214,
  period = "2021",
  geo = "n1",
  classific = "c81",
  category = list(2702)
)
query
}
\seealso{
\code{\link[=get_sidra]{get_sidra()}} and \code{\link[=sidra_plan]{sidra_plan()}}
}
