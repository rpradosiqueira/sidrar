% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{sidra_metadata}
\alias{sidra_metadata}
\title{Retrieve structured SIDRA table metadata}
\usage{
sidra_metadata(
  table,
  refresh = FALSE,
  cache = FALSE,
  ttl = 1800,
  cache_dir = NULL
)
}
\arguments{
\item{table}{A single SIDRA table code.}

\item{refresh}{A single logical value. When caching is enabled, bypass and
replace an existing entry.}

\item{cache}{A single logical value. Cache normalized catalog or metadata
responses on disk. The default is \code{FALSE}, preserving live behavior.}

\item{ttl}{A positive cache lifetime in seconds. The default is 30 minutes.}

\item{cache_dir}{Optional cache directory. By default, the platform-specific
user cache directory for \code{sidrar} is used.}
}
\value{
A named list with six data frames. \code{table} contains \code{table_id},
\code{table_name}, \code{research_name}, \code{subject}, \code{url}, \code{frequency},
\code{period_start}, and \code{period_end}. \code{periods} has the schema documented by
\code{\link[=sidra_periods]{sidra_periods()}}. \code{variables} contains \code{table_id}, \code{variable_id},
\code{variable_name}, \code{unit}, and the character list-column \code{summarized_by}.
\code{classifications} contains \code{table_id}, \code{classification_id},
\code{classification_name}, \code{summarizable}, and the character list-column
\code{summarization_exceptions}. \code{categories} contains \code{table_id},
\code{classification_id}, \code{category_id}, \code{category_name}, \code{unit}, \code{level}, and
\code{category_order}. \code{geographies} contains \code{table_id}, \code{group}, and
\code{level_id}. All API identifiers are character vectors; no raw JSON object
is exposed.
}
\description{
Normalizes the official version 3 metadata and period responses into a
stable collection of base data frames.
}
\details{
\code{sidra_metadata()} requests both the table metadata endpoint and
the table-specific period endpoint. This makes \code{periods} equivalent to
\code{\link[=sidra_periods]{sidra_periods()}} and keeps the table's complete period inventory separate
from its frequency and first/last-period summary. Within each
classification, \code{category_order} is the one-based order in the official
response. Together with \code{level}, it preserves the API's hierarchical
preorder; categories are deliberately not sorted by identifier.
}
\examples{
\dontrun{
metadata <- sidra_metadata(7060)
metadata$variables
metadata$geographies
}
}
\seealso{
\code{\link[=sidra_catalog]{sidra_catalog()}}, \code{\link[=sidra_periods]{sidra_periods()}}, \code{\link[=sidra_locations]{sidra_locations()}}

Other SIDRA discovery functions:
\code{\link[=sidra_catalog]{sidra_catalog()}},
\code{\link[=sidra_locations]{sidra_locations()}},
\code{\link[=sidra_periods]{sidra_periods()}}
}
\author{
Renato Prado Siqueira \email{rpradosiqueira@gmail.com}
}
\concept{SIDRA discovery functions}
\keyword{IBGE}
\keyword{sidra}
