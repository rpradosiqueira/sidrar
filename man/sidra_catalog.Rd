% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{sidra_catalog}
\alias{sidra_catalog}
\title{Discover SIDRA tables}
\usage{
sidra_catalog(refresh = FALSE, cache = FALSE, ttl = 1800, cache_dir = NULL)
}
\arguments{
\item{refresh}{A single logical value. When caching is enabled, bypass and
replace an existing entry.}

\item{cache}{A single logical value. Cache normalized catalog or metadata
responses on disk. The default is \code{FALSE}, preserving live behavior.}

\item{ttl}{A positive cache lifetime in seconds. The default is 30 minutes.}

\item{cache_dir}{Optional cache directory. By default, the platform-specific
user cache directory for \code{sidrar} is used.}
}
\value{
A base \code{\link[=data.frame]{data.frame()}} with columns \code{research_id}, \code{research_name},
\code{table_id}, and \code{table_name}. Identifier columns are character vectors.
Research groups without tables are retained with missing table fields.
}
\description{
Returns the official IBGE aggregate catalog as a rectangular table while
retaining the research group to which each SIDRA table belongs.
}
\details{
The catalog is obtained from the official version 3 aggregate API.
Unknown fields added by the API are ignored. Missing fields are returned
as \code{NA} without changing the documented column types.
}
\examples{
\dontrun{
catalog <- sidra_catalog()
subset(catalog, grepl("prices", table_name, ignore.case = TRUE))
}
}
\seealso{
\code{\link[=search_sidra]{search_sidra()}}, \code{\link[=sidra_metadata]{sidra_metadata()}}

Other SIDRA discovery functions:
\code{\link[=sidra_locations]{sidra_locations()}},
\code{\link[=sidra_metadata]{sidra_metadata()}},
\code{\link[=sidra_periods]{sidra_periods()}}
}
\author{
Renato Prado Siqueira \email{rpradosiqueira@gmail.com}
}
\concept{SIDRA discovery functions}
\keyword{IBGE}
\keyword{sidra}
