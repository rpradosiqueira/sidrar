% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{sidra_locations}
\alias{sidra_locations}
\title{List the locations available for a SIDRA table}
\usage{
sidra_locations(
  table,
  level,
  refresh = FALSE,
  cache = FALSE,
  ttl = 1800,
  cache_dir = NULL
)
}
\arguments{
\item{table}{A single SIDRA table code.}

\item{level}{A single geographic level, with or without the \code{"N"} prefix;
for example, \code{"N6"} or \code{6} for municipalities.}

\item{refresh}{A single logical value. When caching is enabled, bypass and
replace an existing entry.}

\item{cache}{A single logical value. Cache normalized catalog or metadata
responses on disk. The default is \code{FALSE}, preserving live behavior.}

\item{ttl}{A positive cache lifetime in seconds. The default is 30 minutes.}

\item{cache_dir}{Optional cache directory. By default, the platform-specific
user cache directory for \code{sidrar} is used.}
}
\value{
A base \code{\link[=data.frame]{data.frame()}} with columns \code{table_id}, \code{level_id},
\code{level_name}, \code{location_id}, and \code{location_name}. All identifiers are
character vectors.
}
\description{
List the locations available for a SIDRA table
}
\details{
Locations come from the official table- and level-specific version
3 endpoint. Location identifiers are SIDRA territorial identifiers; they
must not be coerced to numeric values or assumed to be census tract codes.
}
\examples{
\dontrun{
sidra_locations(7060, "N6")
}
}
\seealso{
\code{\link[=sidra_metadata]{sidra_metadata()}}

Other SIDRA discovery functions:
\code{\link[=sidra_catalog]{sidra_catalog()}},
\code{\link[=sidra_metadata]{sidra_metadata()}},
\code{\link[=sidra_periods]{sidra_periods()}}
}
\author{
Renato Prado Siqueira \email{rpradosiqueira@gmail.com}
}
\concept{SIDRA discovery functions}
\keyword{IBGE}
\keyword{sidra}
