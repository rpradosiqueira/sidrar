% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{sidra_split}
\alias{sidra_split}
\title{Split an explicit SIDRA query into disjoint batches}
\usage{
sidra_split(
  query,
  by = c("period", "variable", "geo.filter", "category"),
  size,
  index = 1L
)
}
\arguments{
\item{query}{A structured \code{\link[=sidra_query]{sidra_query()}} object. Queries created from
\verb{api =} URLs cannot be split because their original argument structure is
unavailable.}

\item{by}{One of \code{"period"}, \code{"variable"}, \code{"geo.filter"}, or
\code{"category"}.}

\item{size}{Maximum number of selected members in each batch.}

\item{index}{For list-valued \code{geo.filter} and \code{category}, the element to
split. The default is the first element.}
}
\value{
An object of class \code{sidra_batch} containing disjoint \code{queries} in
their original order.
}
\description{
Divides one explicit query dimension without downloading values. The
resulting batches can be executed with \code{\link[=sidra_collect]{sidra_collect()}}. Special selectors
such as \code{all}, \code{allxt}, \code{first}, and \code{last} must first be resolved to
explicit codes. Each vector element must represent one member; embedded
comma lists and period ranges are rejected because their cardinality cannot
be enforced by \code{size}.
}
\details{
Geographic filters can be split only when the query requests one non-Brazil
geographic level. Queries with multiple territorial levels are rejected
because unchanged levels would overlap across batches. Classifications
already resolved in the original URL are reused without another metadata
request.
}
\examples{
query <- sidra_query(
  1612,
  variable = 214,
  period = as.character(2018:2022),
  geo = "Brazil",
  classific = "c81",
  category = list(2702)
)
batches <- sidra_split(query, by = "period", size = 2)
length(batches$queries)
}
\seealso{
\code{\link[=sidra_query]{sidra_query()}}, \code{\link[=sidra_plan]{sidra_plan()}}, \code{\link[=sidra_collect]{sidra_collect()}}
}
