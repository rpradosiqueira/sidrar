% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{sidra_plan}
\alias{sidra_plan}
\title{Estimate the size of a SIDRA query}
\usage{
sidra_plan(query, limit = NULL)
}
\arguments{
\item{query}{A \code{sidra_query} object or an official SIDRA values URL.}

\item{limit}{Optional positive numeric limit used only to classify risk.
No SIDRA limit is assumed by the package.}
}
\value{
A list of class \code{sidra_plan} containing \code{table}, \code{url}, a stable
\code{dimensions} data frame, \code{total_estimated}, \code{limit}, \code{exceeds_limit}, and
\code{risk}. The originating query is retained internally for use by
\code{\link[=sidra_collect]{sidra_collect()}}. \code{total_estimated} is \code{NA} whenever any cardinality is
unknown.
}
\description{
Creates an offline plan from a \code{\link[=sidra_query]{sidra_query()}} object or official SIDRA
values URL. Exact cardinalities are calculated only when they follow from
explicit selections in the URL. Special selections such as \code{all}, \code{allxp},
\code{first}, and \code{last}, territorial views and containment filters remain
unknown until metadata is available.
}
\examples{
query <- sidra_query(
  1612,
  variable = c(214, 215),
  period = c("2020", "2021"),
  geo = "n1",
  classific = "c81",
  category = list(c(2702, 2703))
)
sidra_plan(query, limit = 20)
}
\seealso{
\code{\link[=sidra_query]{sidra_query()}}
}
